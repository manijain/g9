<h2>Create New Account</h2>
<div class="row">
	<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
		  <%= devise_error_messages! %>

		<div class="col-lg-4">
		 <div class="form-group"><%= f.label :name, class: "sr-only" %><br />
		  <%= f.text_field :name, class: "form-control", placeholder: "Name" %></div>

		  <div class="form-group"><%= f.label :email, class: "sr-only" %><br />
		  <%= f.email_field :email, class: "form-control", placeholder: "Email" %></div>

		  <div class="form-group"><%= f.label :password, class: "sr-only" %><br />
		    <%= f.password_field :password, class: "form-control", placeholder: "Password" %>
		  </div>

		  <div class="form-group"><%= f.label :password_confirmation, class: "sr-only" %><br />
		    <%= f.password_field :password_confirmation, class: "form-control", placeholder: "Password confirmation" %></div>

		   <div class="form-group"><%= f.radio_button :gender,'Male', :checked => true %> <label>Male</label> <%= f.radio_button :gender,'Female' %> <label>Female</label></div>
		</div>
		<div class="col-lg-2">
		</div>
		<div class="col-lg-4">
		  <div class="form-group"><%= f.label :address, class: "sr-only" %><br />
		    <%= f.text_area :address, class: "form-control", placeholder: "Address" %></div>

		   <div class="form-group"><%= f.label :city, class: "sr-only" %><br />
		    <%= f.text_field :city, class: "form-control", placeholder: "City" %></div>

		    <div class="form-group"><%= f.label :phone, class: "sr-only" %><br />
		    <%= f.text_field :phone, class: "form-control", placeholder: "Phone#" %></div>

		    <div class="form-group"><%= f.label :occupation, class: "sr-only" %><br />
		    <%= f.text_field :occupation, class: "form-control", placeholder: "Occupation" %></div>
		  <div><%= f.submit "Sign up", class: "btn btn-primary pull-right" %></div>
	<% end %>
	<%= render "devise/shared/links" %>
	</div>
</div>